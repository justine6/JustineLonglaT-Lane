"use client";

import Link from "next/link";
import Image from "next/image";

const links = [
  { label: "Services",      href: "/#services" },
  { label: "Testimonials",  href: "/#testimonials" },
  { label: "Contact",       href: "/#contact" },
  { label: "About",         href: "/about" },
  { label: "Projects",      href: "/projects" },
  { label: "Resume",        href: "/docs/resume.pdf" },
];

export default function Navbar() {
  const linkCls =
    "px-3 py-2 rounded-md text-sm font-medium hover:underline hover:opacity-90";

  return (
    <header className="w-full bg-white/80 dark:bg-gray-950/80 backdrop-blur supports-[backdrop-filter]:bg-white/60 sticky top-0 z-50 border-b border-gray-200/60 dark:border-gray-800">
      <div className="mx-auto max-w-6xl px-4 py-2 flex items-center gap-4">
        <Link href="/" className="flex items-center gap-2">
          <Image
            src="/brand/justine-logo.png"
            alt="Jutellane Solutions"
            width={40}
            height={40}
            priority
            className="rounded"
          />
          <span className="font-semibold">Jutellane Solutions</span>
        </Link>
        <nav className="ml-auto flex items-center gap-1 sm:gap-2">
          {links.map(l => (
            <Link key={l.href} href={l.href} className={linkCls}>
              {l.label}
            </Link>
          ))}
        </nav>
      </div>
    </header>
  );
}
